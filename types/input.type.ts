export type Input = {
  name: string;
  placeholder: string;
  type: string;
  required?: boolean;
};

export type TextArea = {
  placeholder: string;
};

export type SubmitBtn = {
  value: string;
};
